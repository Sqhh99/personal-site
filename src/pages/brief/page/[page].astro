---
import BriefListPage from '../../../components/BriefListPage.astro';
import { columnPageStaticPaths } from '../../../lib/column-pages';

export const getStaticPaths = () => columnPageStaticPaths('brief', 'en');

interface Props {
  page: number;
}

const { page } = Astro.props;
---

<BriefListPage lang="en" currentPage={page} />
